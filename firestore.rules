rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for high-fidelity peer-to-peer real-time synchronization in development/testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
